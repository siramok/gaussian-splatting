name: gaussian_splatting
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - plyfile
  - pip
  - pytorch-cuda
  - torchvision
  - tqdm
  - pip:
    - diff-gaussian-rasterization
    - submodules/simple-knn
    - opencv-python
    - pyvista
    - joblib
